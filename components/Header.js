// components/Header.js

import React from 'react';
import styles from '../styles/Home.module.css';

const Header = () => {
  return (
    <header className={styles.header}>
      <h1>AI Educational Coach</h1>
    </header>
  );
};

export default Header;